{
  "name": "pi-ygrep",
  "version": "0.1.0",
  "description": "Pi extension replacing find with ygrep-indexed search",
  "type": "module",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./src/extension.ts"],
    "skills": ["./skills"]
  },
  "scripts": {
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
